const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({headless:true,executablePath:'/usr/bin/google-chrome',args:['--no-sandbox']});try{const p=await b.newPage({viewport:{width:1400,height:900}});await p.route(/^https:/,r=>r.abort());await p.goto('http://127.0.0.1:8772/');await p.evaluate(()=>updateWorkMode('point'));await p.locator('#map').focus();
const state=()=>p.evaluate(()=>({points:JSON.stringify(positions),center:map.getCenter(),mode,drag:map.dragging.enabled(),space:spacePan}));const a=await state();
await p.keyboard.down('Space');assert.equal((await state()).drag,true);await p.mouse.move(500,400);await p.mouse.down();await p.mouse.move(620,470,{steps:10});await p.keyboard.up('Space');await p.mouse.up();await p.waitForTimeout(400);const c=await state();assert.equal(c.mode,'point');assert.equal(c.drag,false);assert.equal(c.points,a.points);assert.notDeepEqual(c.center,a.center);
await p.keyboard.down('Space');await p.evaluate(()=>window.dispatchEvent(new Event('blur')));assert.equal((await state()).drag,false);await p.keyboard.up('Space');
await p.fill('#latlngInput','');await p.keyboard.press('Space');assert.equal((await state()).space,false);assert.equal(await p.inputValue('#latlngInput'),' ');
await p.locator('#map').focus();await p.mouse.click(500,400);assert.equal(await p.evaluate(()=>positions.length),1);console.log('PASS Space pan: moves map, preserves points, restores on early release/blur, ignores text input, allows subsequent point placement');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
